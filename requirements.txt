# Backend requirements for LogLayer
fastapi
uvicorn
pywebview
pydantic
websockets
psutil
pyinstaller>=6.0
pytest
httpx
requests
ruff
cachetools

# e2e 测试（tests/e2e）：
#   pip install playwright && python -m playwright install chromium
# playwright

