# LogLayer 技术决策记录

> AI 助手必读：本文件记录项目的关键技术决策及其理由，便于理解代码演进和未来维护。

---

## 核心设计原则

| 原则 | 描述 |
|:-----|:-----|
| **简洁优先** | 软件无历史包袱，不维护向后兼容，重构优于修补 |
| **性能至上** | Canvas 虚拟滚动是关键路径，任何优化都不能牺牲性能 |
| **渐进增强** | 新功能优先内置，插件机制作为扩展补充 |
| **自动化测试** | CI/CD 强制质量门禁，测试覆盖率是发布前置条件 |

---

## 决策模板

```markdown
## [ID-001] 决策标题

**日期**: YYYY-MM-DD
**状态**: 已通过 / 已拒绝 / 已弃用
**负责人**: AI Agent / 人类

### 背景
为什么需要做这个决定？

### 方案
- **方案 A**: 描述
- **方案 B**: 描述
- **方案 C**: 描述

### 决策
选择方案 X，原因是...

### 后果
- 正面: ...
- 负面: ...
```

---

## 已记录决策

### TD-005: 查询语言增强 (QueryParser)

**日期**: 2026-03-07
**状态**: 已通过
**类别**: 功能增强

#### 背景
用户需要更强大的日志查询能力，支持复杂的布尔逻辑和模式匹配。

#### 方案
- **方案 A**: 保持简单的字符串匹配
- **方案 B**: 实现完整的 KQL 查询解析器

#### 决策
选择 **方案 B**。实现 QueryParser 类支持：
- 字段查询: `field:value`
- 布尔逻辑: `AND`, `OR`, `NOT`
- 通配符: `*`, `?`
- 正则表达式: `~regex`
- 范围比较: `>`, `<`, `>=`, `<=`
- 分组括号: `(A OR B) AND NOT C`
- in/not in: `level IN [error, warn]`

#### 后果
- **正面**: 支持复杂查询，提升日志分析效率
- **负面**: 增加代码复杂度

---

### TD-006: 视图管理系统 (Saved Views)

**日期**: 2026-03-07
**状态**: 已通过
**类别**: 功能增强

#### 背景
用户需要保存和恢复特定的过滤/高亮配置，类似 Kibana 和 Grafana 的 Saved Views。

#### 方案
- **方案 A**: 仅保存当前会话
- **方案 B**: 持久化保存视图到文件

#### 决策
选择 **方案 B**。创建 ViewManager 类支持：
- 保存/加载视图配置
- 导出/导入视图 (JSON 格式)
- 自动保存工作区状态

#### 后果
- **正面**: 提升工作流效率，便于团队共享
- **负面**: 需要持久化存储逻辑

---

### TD-007: 时间线直方图 (TimelineHistogram)

**日期**: 2026-03-07
**状态**: 已通过
**类别**: 可视化

#### 背景
用户需要直观地了解日志在时间上的分布情况，特别是按级别区分。

#### 方案
- **方案 A**: 不提供时间分布
- **方案 B**: 实现时间线直方图组件

#### 决策
选择 **方案 B**。创建 TimelineLayer 和 TimelineHistogram 组件：
- 按时间桶统计日志数量
- 按日志级别颜色编码
- 支持缩放和滚动

#### 后果
- **正面**: 快速定位时间异常
- **负面**: 增加渲染开销

---

### TD-008: 标签提取层 (LabelLayer)

**日期**: 2026-03-07
**状态**: 已通过
**类别**: 功能增强

#### 背景
用户需要类似 Loki 的标签系统来自动提取和过滤日志中的关键信息。

#### 方案
- **方案 A**: 手动定义标签
- **方案 B**: 自动提取标签

#### 决策
选择 **方案 B**。创建 LabelLayer 支持：
- 自动识别常见格式: `key=value`, `key:value`, JSON
- 标签统计和过滤
- 类级别编译模式优化性能

#### 后果
- **正面**: 自动分类日志，快速定位问题
- **负面**: 正则表达式可能影响性能 (已优化缓存)

---

### TD-009: 后端模块化重构

**日期**: 2026-03-07
**状态**: 已通过
**类别**: 架构

#### 背景
main.py 过于臃肿 (600+ 行)，难以维护。

#### 方案
- **方案 A**: 保持单体
- **方案 B**: 提取到独立模块

#### 决策
选择 **方案 B**。重构为：
- `api_routes.py`: 所有 REST 端点
- `websocket_manager.py`: WebSocket 管理
- `views.py`: 视图管理系统
- `workers.py`: 后台工作线程

#### 后果
- **正面**: 代码可维护性提升
- **负面**: 需要管理更多文件

---

### TD-010: CI/CD 自动化

**日期**: 2026-03-07
**状态**: 已通过
**类别**: 开发效率

#### 背景
需要自动化测试和代码质量检查。

#### 方案
- **方案 A**: 手动测试
- **方案 B**: GitHub Actions 自动化

#### 决策
选择 **方案 B**。配置：
- GitHub Actions workflow: pytest + build
- Ruff: Python linting
- Prettier + ESLint: 前端代码格式

#### 后果
- **正面**: 自动化质量检查，减少人工错误
- **负面**: 需要维护配置文件

---

### TD-004: LRU 缓存性能优化

**日期**: 2026-03-05
**状态**: 已通过
**类别**: 性能优化

#### 背景
原有的 LRUCache 使用 list 维护访问顺序，导致 `__getitem__` 和 `__setitem__` 操作中的 `remove()` 为 O(n) 时间复杂度。在高频访问的日志行缓存场景下，这会导致性能瓶颈。

#### 方案
- **方案 A**: 保持原有 list 实现
- **方案 B**: 使用 collections.OrderedDict 实现 O(1) 操作

#### 决策
选择 **方案 B**。使用 OrderedDict 的 `move_to_end()` 方法实现 O(1) 的访问更新，`popitem(last=False)` 实现 O(1) 的淘汰最旧项。

#### 后果
- **正面**: 缓存操作从 O(n) 优化到 O(1)，大文件滚动性能提升
- **负面**: 无，OrderedDict 是 Python 标准库

---

### TD-003: 日志等级预设过滤

**日期**: 2026-03-05
**状态**: 已通过
**类别**: 用户体验

#### 背景
用户需要快速切换常见的日志等级过滤组合（如仅错误、生产环境等），但每次都需要手动选择多个等级。

#### 方案
- **方案 A**: 保持手动选择
- **方案 B**: 添加预设模式 (Preset) 下拉选项

#### 决策
选择 **方案 B**。在 LevelLayer 中添加 preset 输入，支持 custom/errors_only/warnings_and_above/production/development/quiet 等预设。

#### 后果
- **正面**: 提升用户体验，快速切换常见过滤场景
- **负面**: 增加少量代码复杂度

---

### TD-002: 导出功能元数据增强

**日期**: 2026-03-05
**状态**: 已通过
**类别**: 功能增强

#### 背景
原有的 JSON 导出格式缺少完整的元数据信息，不利于后续分析和审计。

#### 方案
- **方案 A**: 保持简单格式
- **方案 B**: 添加完整的 metadata 块 (源文件、导出时间、过滤统计等)

#### 决策
选择 **方案 B**。在导出 JSON 中添加 metadata 块，包含 source_file、source_path、exported_at、format_version、truncated 等信息。

#### 后果
- **正面**: 导出文件更具可读性和可追溯性
- **负面**: 文件大小略微增加

---

### TD-001: 虚拟滚动实现方案

**日期**: 2026-02-12
**状态**: 已通过
**类别**: 性能优化

#### 背景
LogLayer 需要支持百万行级别的日志文件，传统的 DOM 渲染会导致浏览器崩溃。

#### 方案
- **方案 A (DOM)**: 使用 React 虚拟列表库
- **方案 B (Canvas)**: 自定义 Canvas 渲染 + 虚拟滚动

#### 决策
选择 **方案 B**。

#### 理由
1. Canvas 渲染性能更好 (60fps vs 30fps)
2. 完全控制渲染逻辑，便于优化
3. 高 DPI 支持更精细

#### 后果
- 需要手动实现滚动同步
- 文本选择需要额外处理
- 学习曲线略高

---

### TD-002: 图层系统架构

**日期**: 2026-02-07
**状态**: 已通过
**类别**: 架构

#### 背景
需要统一管理日志的过滤、高亮、标记等功能。

#### 方案
- **方案 A**: 前端独立实现
- **方案 B**: 后端处理 + 前端渲染

#### 决策
选择 **方案 B**。

#### 理由
1. 后端处理可以利用多核 CPU
2. 状态同步更可靠
3. 支持插件扩展

---

### TD-003: mmap 索引策略

**日期**: 2026-02-04
**状态**: 已通过
**类别**: 性能

#### 背景
GB 级文件无法一次性加载到内存。

#### 方案
- **方案 A**: 流式读取
- **方案 B**: mmap 内存映射

#### 决策
选择 **方案 B**。

#### 理由
1. OS 负责分页管理
2. 随机访问性能好
3. 代码实现简单

---

### TD-004: 常量管理系统

**日期**: 2026-02-21
**状态**: 已通过
**类别**: 代码质量

#### 背景
LogViewer 组件中存在大量硬编码值，难以维护和统一修改。

#### 方案
- **方案 A**: 分散在各组件中
- **方案 B**: 集中到 constants.ts

#### 决策
选择 **方案 B**。

#### 实施
```typescript
// frontend/src/constants.ts
export const LOG_VIEWER = {
  LINE_HEIGHT: 20,
  GUTTER_WIDTH: 80,
  // ...
} as const;
```

---

### TD-005: 错误处理策略

**日期**: 2026-02-21
**状态**: 已通过
**类别**: 稳定性

#### 背景
Canvas 渲染可能抛出异常，导致整个应用崩溃。

#### 方案
- **方案 A**: 全局 try-catch
- **方案 B**: ErrorBoundary 组件

#### 决策
选择 **方案 B**。

#### 理由
1. React 官方推荐
2. 局部错误不影响整体
3. 提供友好的错误 UI

---

### TD-006: 键盘导航设计

**日期**: 2026-02-21
**状态**: 已通过
**类别**: UX

#### 背景
需要提升高级用户的操作效率，减少鼠标依赖。

#### 方案
- **方案 A**: 模仿 VS Code
- **方案 B**: 自定义快捷键

#### 决策
选择 **方案 A (参考 VS Code)**。

#### 理由
1. 用户熟悉
2. 降低学习成本
3. 有成熟的交互模式

#### 快捷键列表
| 快捷键 | 功能 |
|:-------|:-----|
| Ctrl+G | 跳转到行 |
| Ctrl+Shift+L | 选中当前行 |
| Ctrl+Enter | 跳转到选中行 |
| Alt+↑/↓ | 移动选区 |
| Ctrl+A | 全选 |

---

### TD-007: 颜色管理系统

**日期**: 2026-02-21
**状态**: 已通过
**类别**: 代码质量

#### 背景
Canvas 渲染中使用大量魔法颜色值，难以维护和主题切换。

#### 方案
- **方案 A**: 分散硬编码
- **方案 B**: COLORS 常量对象

#### 决策
选择 **方案 B**。

#### 实施
```typescript
export const COLORS = {
  DARK: {
    BACKGROUND: '#1e1e1e',
    SELECTION: 'rgba(245, 158, 11, 0.45)',
    // ...
  },
  LIGHT: {
    // 亮色主题
  },
} as const;
```

---

### TD-008: TypeScript 类型策略

**日期**: 2026-02-21
**状态**: 已完成
**类别**: 代码质量

#### 背景
项目中存在 `any` 类型和类型断言，降低了类型安全性。

#### 方案
- **方案 A**: 容忍 any
- **方案 B**: 严格类型检查 + 逐步修复

#### 决策
选择 **方案 B**。

#### 完成情况
- [x] App.tsx ProcessedCache 类型 - 已修复
- [x] SearchConfig 类型兼容 - 已修复
- [x] Toast.tsx 属性缺失 - 已修复
- [x] UnifiedPanel.tsx 类型错误 - 已修复

TypeScript 编译通过，无类型错误。

---

## 待决策

### TD-011: 插件系统架构

**状态**: 待讨论
**类别**: 扩展性

#### 选项
- **方案 A**: Python 插件 (同内置图层)
- **方案 B**: JavaScript 插件
- **方案 C**: WebAssembly 插件

---

### TD-013: 高亮渲染垂直对齐策略

**日期**: 2026-03-12
**状态**: 已通过
**类别**: UI 渲染一致性

#### 背景
Canvas 渲染中，搜索高亮/图层高亮的文字背景高度与选中高亮（selection）不一致，导致视觉上高亮背景比选中区域低 4px，出现错位感。

#### 问题分析
```javascript
// 选中高亮 - 使用 y 作为起始位置 (正确)
ctx.fillRect(gutterWidth + selX - safeScrollLeft, y, selW, lineHeight);

// 搜索高亮 - 使用 startY - lineHeight/2 (错误)
// startY = y + lineHeight/2 + 4
// 所以 hlY = y + 4，比选中高亮低 4px
const hlY = startY - lineHeight / 2;
```

#### 方案
- **方案 A**: 保持现状，接受视觉错位
- **方案 B**: 统一所有高亮背景使用行起始位置 `y`

#### 决策
选择 **方案 B**。

#### 实施
修复 `LogViewer.tsx` 中的 `renderText` 函数：
- 搜索高亮 (`h.isSearch || h.color === '#facc15'`): `hlY = y`
- 图层高亮 (else 分支): `hlY = y`

所有高亮背景现在统一从行顶部 (`y`) 开始，高度为 `lineHeight`，与选中高亮完全对齐。

#### 后果
- **正面**: 视觉一致性提升，高亮背景与选中区域对齐
- **负面**: 无

---

### TD-012: AI 集成方向

**状态**: 待讨论
**类别**: 功能增强

#### 选项
- **方案 A**: 仅本地 heuristic 分析
- **方案 B**: 云端 LLM 集成
- **方案 C**: 混合模式 (本地优先，失败时回退云端)

---

## 废弃决策

### TD-000: 初始渲染方案 (已废弃)

**原因**: 性能不足以支持大文件，已被 TD-001 替代

---

## TD-014: 类型同步策略

**日期**: 2026-03-20
**状态**: 已通过
**类别**: 架构

### 背景
前后端类型需要保持同步，手动同步容易遗漏导致 API 契约不一致。

### 方案
- **方案 A**: 自动生成 (pydantic-to-typescript)
- **方案 B**: 手动同步 + CI 验证

### 决策
选择 **方案 B**。

### 理由
1. 当前类型数量可控 (~30)
2. 自动生成增加构建复杂度
3. CI 验证足以捕获不一致

### 后果
- **正面**: 类型一致有保障
- **负面**: 需要手动维护 TYPE_SYNC.md

---

## TD-015: API 响应标准

**日期**: 2026-03-20
**状态**: 已通过
**类别**: API 设计

### 背景
API 响应格式不一致，错误处理分散。

### 方案
- **方案 A**: 保持现状
- **方案 B**: 统一响应格式 `{success, data?, error?}`

### 决策
选择 **方案 B**。

### 实施
```typescript
interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: { code: string; message: string; details?: unknown };
}
```

### 后果
- **正面**: 统一错误处理
- **负面**: 需要逐步迁移

---

## TD-016: Storage Manager 架构

**日期**: 2026-03-20
**状态**: 已通过
**类别**: 状态管理

### 背景
localStorage 访问分散在 9 个文件中，缺乏类型安全。

### 方案
- **方案 A**: 保持现状
- **方案 B**: 创建 StorageManager 统一管理

### 决策
选择 **方案 B**。

### 实施
```typescript
const StorageManager = {
  get<K extends StorageKey>(key: K): StorageDefaults[K];
  set<K extends StorageKey>(key: K, value: StorageDefaults[K]): void;
}
```

### 后果
- **正面**: 类型安全、易于迁移
- **负面**: 需要逐步迁移现有代码

---

*最后更新: 2026-03-14*
