# LogLayer 帮助文档

## 快速开始

### 打开文件
- 拖放日志文件到窗口
- 使用 `Ctrl+O` 打开文件选择器
- 使用 `Ctrl+Shift+O` 打开文件夹

### 图层系统
LogLayer 使用图层系统进行日志分析，原始文件永远不会被修改。

#### FILTER (过滤图层)
只显示匹配的行，支持正则表达式。多图层叠加时按顺序依次过滤。

#### HIGHLIGHT (高亮图层)
用指定颜色标记匹配的行，不影响其他行显示。适合标记关键信息。

## 快捷键

| 功能 | 快捷键 |
|------|--------|
| 打开文件 | `Ctrl+O` |
| 打开文件夹 | `Ctrl+Shift+O` |
| 查找 | `Ctrl+F` |
| 跳转到行 | `Ctrl+G` |
| 下一个匹配 | `F3` |
| 上一个匹配 | `Shift+F3` |
| 下一个书签 | `F2` |
| 上一个书签 | `Shift+F2` |
| 新建图层 | `Ctrl+Shift+L` |
| 打开设置 | `Ctrl+,` |
| 向右分屏 | `Ctrl+\` |
| 关闭分屏 | `Ctrl+W` |
| 实时监视 | `Ctrl+Shift+T` |
| 命令面板 | `Ctrl+P` |

## 搜索技巧

### 正则表达式
启用 Regex 模式，使用 `ERROR|WARN` 匹配多个关键词

### 大小写敏感
默认不区分大小写，点击 Case 可切换为精确匹配

### 全局搜索
搜索使用 ripgrep 引擎，GB 级文件也能瞬间完成

## 书签

- 点击行号左侧添加书签
- 在侧边栏书签面板中添加备注
- 使用 F2 / Shift+F2 快速跳转

---

LogLayer v5.1 · 高效日志分析体验